{
  "name": "blog",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "publish": "lerna run build && lerna publish",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap"
  }
}
